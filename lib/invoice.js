'use strict';

// The content of this file was automatically generated

var Promise    = require('promise');
var Base       = require('basejs');
var ProcessOut = require('./processout');
var Response   = require('./networking/response');
var Request    = require('./networking/request');

var Invoice = module.exports = Base.extend({
    _client: null,

    /**
     * ID of the invoice
     * @type {string}
     */
    _id: null,

    /**
     * Project to which the invoice belongs
     * @type {object}
     */
    _project: null,

    /**
     * ID of the project to which the invoice belongs
     * @type {string}
     */
    _projectId: null,

    /**
     * Transaction generated by the invoice
     * @type {object}
     */
    _transaction: null,

    /**
     * ID of the transaction generated by the invoice
     * @type {string}
     */
    _transactionId: null,

    /**
     * Customer linked to the invoice, if any
     * @type {object}
     */
    _customer: null,

    /**
     * ID of the customer linked to the invoice, if any
     * @type {string}
     */
    _customerId: null,

    /**
     * Subscription to which the invoice is linked to, if any
     * @type {object}
     */
    _subscription: null,

    /**
     * ID of the subscription to which the invoice is linked to, if any
     * @type {string}
     */
    _subscriptionId: null,

    /**
     * Token used to pay the invoice, if any
     * @type {object}
     */
    _token: null,

    /**
     * ID of the token used to pay the invoice, if any
     * @type {string}
     */
    _tokenId: null,

    /**
     * Details of the invoice
     * @type {list}
     */
    _details: null,

    /**
     * URL to which you may redirect your customer to proceed with the payment
     * @type {string}
     */
    _url: null,

    /**
     * Name of the invoice
     * @type {string}
     */
    _name: null,

    /**
     * Amount to be paid
     * @type {decimal}
     */
    _amount: null,

    /**
     * Currency of the invoice
     * @type {string}
     */
    _currency: null,

    /**
     * Statement to be shown on the bank statement of your customer
     * @type {string}
     */
    _statementDescriptor: null,

    /**
     * Support phone number shown on the customer's bank statement
     * @type {string}
     */
    _statementDescriptorPhone: null,

    /**
     * City shown on the customer's bank statement
     * @type {string}
     */
    _statementDescriptorCity: null,

    /**
     * Your company name shown on the customer's bank statement
     * @type {string}
     */
    _statementDescriptorCompany: null,

    /**
     * URL shown on the customer's bank statement
     * @type {string}
     */
    _statementDescriptorUrl: null,

    /**
     * Metadata related to the invoice, in the form of a dictionary (key-value pair)
     * @type {dictionary}
     */
    _metadata: null,

    /**
     * URL where the customer will be redirected upon payment
     * @type {string}
     */
    _returnUrl: null,

    /**
     * URL where the customer will be redirected if the payment was canceled
     * @type {string}
     */
    _cancelUrl: null,

    /**
     * Define whether or not the invoice is in sandbox environment
     * @type {boolean}
     */
    _sandbox: null,

    /**
     * Date at which the invoice was created
     * @type {string}
     */
    _createdAt: null,

    /**
     * Invoice constructor
     * @param {ProcessOut} client
     * @param {array} prefill (optional)
     */
    constructor: function(client, prefill) {
        if (typeof client === 'undefined')
            throw new Error("The Invoice object must be instanciated with the ProcessOut client. You can create one using new ProcessOut('project-id', 'project-secret').newInvoice()");

        this._client = client;
        if (typeof prefill !== 'undefined')
            this.fillWithData(prefill);
    },

    /**
     * Get Id
     * ID of the invoice
     * @return {string}
     */
    getId: function() {
        return this._id;
    },

    /**
     * Set Id
     * ID of the invoice
     * @param {string} val
     * @return {Invoice}
     */
    setId: function(val) {
        this._id = val;
        return this;
    },

    /**
     * Get Project
     * Project to which the invoice belongs
     * @return {object}
     */
    getProject: function() {
        return this._project;
    },

    /**
     * Set Project
     * Project to which the invoice belongs
     * @param {object} val
     * @return {Invoice}
     */
    setProject: function(val) {
        if (typeof val.getClass === "function" &&
            val.getClass() == this._client.newProject().getClass())
            this._project = val;
        else {
            var obj = this._client.newProject();
            obj.fillWithData(val);
            this._project = obj;
        }
        return this;
    },

    /**
     * Get ProjectId
     * ID of the project to which the invoice belongs
     * @return {string}
     */
    getProjectId: function() {
        return this._projectId;
    },

    /**
     * Set ProjectId
     * ID of the project to which the invoice belongs
     * @param {string} val
     * @return {Invoice}
     */
    setProjectId: function(val) {
        this._projectId = val;
        return this;
    },

    /**
     * Get Transaction
     * Transaction generated by the invoice
     * @return {object}
     */
    getTransaction: function() {
        return this._transaction;
    },

    /**
     * Set Transaction
     * Transaction generated by the invoice
     * @param {object} val
     * @return {Invoice}
     */
    setTransaction: function(val) {
        if (typeof val.getClass === "function" &&
            val.getClass() == this._client.newTransaction().getClass())
            this._transaction = val;
        else {
            var obj = this._client.newTransaction();
            obj.fillWithData(val);
            this._transaction = obj;
        }
        return this;
    },

    /**
     * Get TransactionId
     * ID of the transaction generated by the invoice
     * @return {string}
     */
    getTransactionId: function() {
        return this._transactionId;
    },

    /**
     * Set TransactionId
     * ID of the transaction generated by the invoice
     * @param {string} val
     * @return {Invoice}
     */
    setTransactionId: function(val) {
        this._transactionId = val;
        return this;
    },

    /**
     * Get Customer
     * Customer linked to the invoice, if any
     * @return {object}
     */
    getCustomer: function() {
        return this._customer;
    },

    /**
     * Set Customer
     * Customer linked to the invoice, if any
     * @param {object} val
     * @return {Invoice}
     */
    setCustomer: function(val) {
        if (typeof val.getClass === "function" &&
            val.getClass() == this._client.newCustomer().getClass())
            this._customer = val;
        else {
            var obj = this._client.newCustomer();
            obj.fillWithData(val);
            this._customer = obj;
        }
        return this;
    },

    /**
     * Get CustomerId
     * ID of the customer linked to the invoice, if any
     * @return {string}
     */
    getCustomerId: function() {
        return this._customerId;
    },

    /**
     * Set CustomerId
     * ID of the customer linked to the invoice, if any
     * @param {string} val
     * @return {Invoice}
     */
    setCustomerId: function(val) {
        this._customerId = val;
        return this;
    },

    /**
     * Get Subscription
     * Subscription to which the invoice is linked to, if any
     * @return {object}
     */
    getSubscription: function() {
        return this._subscription;
    },

    /**
     * Set Subscription
     * Subscription to which the invoice is linked to, if any
     * @param {object} val
     * @return {Invoice}
     */
    setSubscription: function(val) {
        if (typeof val.getClass === "function" &&
            val.getClass() == this._client.newSubscription().getClass())
            this._subscription = val;
        else {
            var obj = this._client.newSubscription();
            obj.fillWithData(val);
            this._subscription = obj;
        }
        return this;
    },

    /**
     * Get SubscriptionId
     * ID of the subscription to which the invoice is linked to, if any
     * @return {string}
     */
    getSubscriptionId: function() {
        return this._subscriptionId;
    },

    /**
     * Set SubscriptionId
     * ID of the subscription to which the invoice is linked to, if any
     * @param {string} val
     * @return {Invoice}
     */
    setSubscriptionId: function(val) {
        this._subscriptionId = val;
        return this;
    },

    /**
     * Get Token
     * Token used to pay the invoice, if any
     * @return {object}
     */
    getToken: function() {
        return this._token;
    },

    /**
     * Set Token
     * Token used to pay the invoice, if any
     * @param {object} val
     * @return {Invoice}
     */
    setToken: function(val) {
        if (typeof val.getClass === "function" &&
            val.getClass() == this._client.newToken().getClass())
            this._token = val;
        else {
            var obj = this._client.newToken();
            obj.fillWithData(val);
            this._token = obj;
        }
        return this;
    },

    /**
     * Get TokenId
     * ID of the token used to pay the invoice, if any
     * @return {string}
     */
    getTokenId: function() {
        return this._tokenId;
    },

    /**
     * Set TokenId
     * ID of the token used to pay the invoice, if any
     * @param {string} val
     * @return {Invoice}
     */
    setTokenId: function(val) {
        this._tokenId = val;
        return this;
    },

    /**
     * Get Details
     * Details of the invoice
     * @return {array}
     */
    getDetails: function() {
        return this._details;
    },

    /**
     * Set Details
     * Details of the invoice
     * @param {array} val
     * @return {Invoice}
     */
    setDetails: function(val) {
        if (val.length > 0 && typeof val[0] === 'object')
            this._details = val;
        else {
            var a = [];
            for (var i = val.length; i--;) {
                var obj = this._client.newInvoiceDetail();
                obj.fillWithData(val);
                a.push(obj);
            }
            this._details = a;
        }
        return this;
    },

    /**
     * Get Url
     * URL to which you may redirect your customer to proceed with the payment
     * @return {string}
     */
    getUrl: function() {
        return this._url;
    },

    /**
     * Set Url
     * URL to which you may redirect your customer to proceed with the payment
     * @param {string} val
     * @return {Invoice}
     */
    setUrl: function(val) {
        this._url = val;
        return this;
    },

    /**
     * Get Name
     * Name of the invoice
     * @return {string}
     */
    getName: function() {
        return this._name;
    },

    /**
     * Set Name
     * Name of the invoice
     * @param {string} val
     * @return {Invoice}
     */
    setName: function(val) {
        this._name = val;
        return this;
    },

    /**
     * Get Amount
     * Amount to be paid
     * @return {string}
     */
    getAmount: function() {
        return this._amount;
    },

    /**
     * Set Amount
     * Amount to be paid
     * @param {string} val
     * @return {Invoice}
     */
    setAmount: function(val) {
        this._amount = val;
        return this;
    },

    /**
     * Get Currency
     * Currency of the invoice
     * @return {string}
     */
    getCurrency: function() {
        return this._currency;
    },

    /**
     * Set Currency
     * Currency of the invoice
     * @param {string} val
     * @return {Invoice}
     */
    setCurrency: function(val) {
        this._currency = val;
        return this;
    },

    /**
     * Get StatementDescriptor
     * Statement to be shown on the bank statement of your customer
     * @return {string}
     */
    getStatementDescriptor: function() {
        return this._statementDescriptor;
    },

    /**
     * Set StatementDescriptor
     * Statement to be shown on the bank statement of your customer
     * @param {string} val
     * @return {Invoice}
     */
    setStatementDescriptor: function(val) {
        this._statementDescriptor = val;
        return this;
    },

    /**
     * Get StatementDescriptorPhone
     * Support phone number shown on the customer's bank statement
     * @return {string}
     */
    getStatementDescriptorPhone: function() {
        return this._statementDescriptorPhone;
    },

    /**
     * Set StatementDescriptorPhone
     * Support phone number shown on the customer's bank statement
     * @param {string} val
     * @return {Invoice}
     */
    setStatementDescriptorPhone: function(val) {
        this._statementDescriptorPhone = val;
        return this;
    },

    /**
     * Get StatementDescriptorCity
     * City shown on the customer's bank statement
     * @return {string}
     */
    getStatementDescriptorCity: function() {
        return this._statementDescriptorCity;
    },

    /**
     * Set StatementDescriptorCity
     * City shown on the customer's bank statement
     * @param {string} val
     * @return {Invoice}
     */
    setStatementDescriptorCity: function(val) {
        this._statementDescriptorCity = val;
        return this;
    },

    /**
     * Get StatementDescriptorCompany
     * Your company name shown on the customer's bank statement
     * @return {string}
     */
    getStatementDescriptorCompany: function() {
        return this._statementDescriptorCompany;
    },

    /**
     * Set StatementDescriptorCompany
     * Your company name shown on the customer's bank statement
     * @param {string} val
     * @return {Invoice}
     */
    setStatementDescriptorCompany: function(val) {
        this._statementDescriptorCompany = val;
        return this;
    },

    /**
     * Get StatementDescriptorUrl
     * URL shown on the customer's bank statement
     * @return {string}
     */
    getStatementDescriptorUrl: function() {
        return this._statementDescriptorUrl;
    },

    /**
     * Set StatementDescriptorUrl
     * URL shown on the customer's bank statement
     * @param {string} val
     * @return {Invoice}
     */
    setStatementDescriptorUrl: function(val) {
        this._statementDescriptorUrl = val;
        return this;
    },

    /**
     * Get Metadata
     * Metadata related to the invoice, in the form of a dictionary (key-value pair)
     * @return {object}
     */
    getMetadata: function() {
        return this._metadata;
    },

    /**
     * Set Metadata
     * Metadata related to the invoice, in the form of a dictionary (key-value pair)
     * @param {object} val
     * @return {Invoice}
     */
    setMetadata: function(val) {
        this._metadata = val;
        return this;
    },

    /**
     * Get ReturnUrl
     * URL where the customer will be redirected upon payment
     * @return {string}
     */
    getReturnUrl: function() {
        return this._returnUrl;
    },

    /**
     * Set ReturnUrl
     * URL where the customer will be redirected upon payment
     * @param {string} val
     * @return {Invoice}
     */
    setReturnUrl: function(val) {
        this._returnUrl = val;
        return this;
    },

    /**
     * Get CancelUrl
     * URL where the customer will be redirected if the payment was canceled
     * @return {string}
     */
    getCancelUrl: function() {
        return this._cancelUrl;
    },

    /**
     * Set CancelUrl
     * URL where the customer will be redirected if the payment was canceled
     * @param {string} val
     * @return {Invoice}
     */
    setCancelUrl: function(val) {
        this._cancelUrl = val;
        return this;
    },

    /**
     * Get Sandbox
     * Define whether or not the invoice is in sandbox environment
     * @return {bool}
     */
    getSandbox: function() {
        return this._sandbox;
    },

    /**
     * Set Sandbox
     * Define whether or not the invoice is in sandbox environment
     * @param {bool} val
     * @return {Invoice}
     */
    setSandbox: function(val) {
        this._sandbox = val;
        return this;
    },

    /**
     * Get CreatedAt
     * Date at which the invoice was created
     * @return {string}
     */
    getCreatedAt: function() {
        return this._createdAt;
    },

    /**
     * Set CreatedAt
     * Date at which the invoice was created
     * @param {string} val
     * @return {Invoice}
     */
    setCreatedAt: function(val) {
        this._createdAt = val;
        return this;
    },

    /**
     * Fills the current object with the new values pulled from the data
     * @param  {array} data
     * @return {Invoice}
     */
    fillWithData: function(data) {
        if (data["id"])
            this.setId(data["id"]);
        if (data["project"])
            this.setProject(data["project"]);
        if (data["project_id"])
            this.setProjectId(data["project_id"]);
        if (data["transaction"])
            this.setTransaction(data["transaction"]);
        if (data["transaction_id"])
            this.setTransactionId(data["transaction_id"]);
        if (data["customer"])
            this.setCustomer(data["customer"]);
        if (data["customer_id"])
            this.setCustomerId(data["customer_id"]);
        if (data["subscription"])
            this.setSubscription(data["subscription"]);
        if (data["subscription_id"])
            this.setSubscriptionId(data["subscription_id"]);
        if (data["token"])
            this.setToken(data["token"]);
        if (data["token_id"])
            this.setTokenId(data["token_id"]);
        if (data["details"])
            this.setDetails(data["details"]);
        if (data["url"])
            this.setUrl(data["url"]);
        if (data["name"])
            this.setName(data["name"]);
        if (data["amount"])
            this.setAmount(data["amount"]);
        if (data["currency"])
            this.setCurrency(data["currency"]);
        if (data["statement_descriptor"])
            this.setStatementDescriptor(data["statement_descriptor"]);
        if (data["statement_descriptor_phone"])
            this.setStatementDescriptorPhone(data["statement_descriptor_phone"]);
        if (data["statement_descriptor_city"])
            this.setStatementDescriptorCity(data["statement_descriptor_city"]);
        if (data["statement_descriptor_company"])
            this.setStatementDescriptorCompany(data["statement_descriptor_company"]);
        if (data["statement_descriptor_url"])
            this.setStatementDescriptorUrl(data["statement_descriptor_url"]);
        if (data["metadata"])
            this.setMetadata(data["metadata"]);
        if (data["return_url"])
            this.setReturnUrl(data["return_url"]);
        if (data["cancel_url"])
            this.setCancelUrl(data["cancel_url"]);
        if (data["sandbox"])
            this.setSandbox(data["sandbox"]);
        if (data["created_at"])
            this.setCreatedAt(data["created_at"]);
        return this;
    },

    /**
     * Authorize the invoice using the given source (customer or token)
	 * @param string source
     * @param {object} options
     * @return {Transaction}
     */
    authorize: function(source, options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices/" + encodeURI(this.getId()) + "/authorize";

        var data = {
			'synchronous': (options['synchronous']) ? options['synchronous'] : null, 
			'retry_drop_liability_shift': (options['retry_drop_liability_shift']) ? options['retry_drop_liability_shift'] : null, 
			'capture_amount': (options['capture_amount']) ? options['capture_amount'] : null, 
			'source': source
        };

        var req = request.post(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var body = response.body;
                body = body['transaction'];
                var obj = cur._client.newTransaction();
                returnValues.push(obj.fillWithData(body));

                return resolve.apply(this, returnValues);
            });
        });
    },
    /**
     * Capture the invoice using the given source (customer or token)
	 * @param string source
     * @param {object} options
     * @return {Transaction}
     */
    capture: function(source, options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices/" + encodeURI(this.getId()) + "/capture";

        var data = {
			'authorize_only': (options['authorize_only']) ? options['authorize_only'] : null, 
			'synchronous': (options['synchronous']) ? options['synchronous'] : null, 
			'retry_drop_liability_shift': (options['retry_drop_liability_shift']) ? options['retry_drop_liability_shift'] : null, 
			'capture_amount': (options['capture_amount']) ? options['capture_amount'] : null, 
			'source': source
        };

        var req = request.post(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var body = response.body;
                body = body['transaction'];
                var obj = cur._client.newTransaction();
                returnValues.push(obj.fillWithData(body));

                return resolve.apply(this, returnValues);
            });
        });
    },
    /**
     * Get the customer linked to the invoice.

     * @param {object} options
     * @return {Customer}
     */
    fetchCustomer: function(options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices/" + encodeURI(this.getId()) + "/customers";

        var data = {

        };

        var req = request.get(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var body = response.body;
                body = body['customer'];
                var obj = cur._client.newCustomer();
                returnValues.push(obj.fillWithData(body));

                return resolve.apply(this, returnValues);
            });
        });
    },
    /**
     * Assign a customer to the invoice.
	 * @param string customerId
     * @param {object} options
     * @return {Customer}
     */
    assignCustomer: function(customerId, options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices/" + encodeURI(this.getId()) + "/customers";

        var data = {
			'customer_id': customerId
        };

        var req = request.post(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var body = response.body;
                body = body['customer'];
                var obj = cur._client.newCustomer();
                returnValues.push(obj.fillWithData(body));

                return resolve.apply(this, returnValues);
            });
        });
    },
    /**
     * Initiate a 3-D Secure authentication
	 * @param string source
     * @param {object} options
     * @return {CustomerAction}
     */
    initiateThreeDS: function(source, options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices/" + encodeURI(this.getId()) + "/three-d-s";

        var data = {
			'source': source
        };

        var req = request.post(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var body = response.body;
                body = body['customer_action'];
                var obj = cur._client.newCustomerAction();
                returnValues.push(obj.fillWithData(body));

                return resolve.apply(this, returnValues);
            });
        });
    },
    /**
     * Get the transaction of the invoice.

     * @param {object} options
     * @return {Transaction}
     */
    fetchTransaction: function(options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices/" + encodeURI(this.getId()) + "/transactions";

        var data = {

        };

        var req = request.get(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var body = response.body;
                body = body['transaction'];
                var obj = cur._client.newTransaction();
                returnValues.push(obj.fillWithData(body));

                return resolve.apply(this, returnValues);
            });
        });
    },
    /**
     * Void the invoice

     * @param {object} options
     * @return {Transaction}
     */
    void: function(options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices/" + encodeURI(this.getId()) + "/void";

        var data = {

        };

        var req = request.post(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var body = response.body;
                body = body['transaction'];
                var obj = cur._client.newTransaction();
                returnValues.push(obj.fillWithData(body));

                return resolve.apply(this, returnValues);
            });
        });
    },
    /**
     * Get all the invoices.
     * 
     * @param {object} options
     * @return {array}
     */
    all: function(options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices";

        var data = {

        };

        var req = request.get(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var a    = [];
                var body = response.body['invoices'];
                for (var i = body.length; i--;) {
                    var tmp = cur._client.newInvoice();
                    tmp.fillWithData(body[i]);
                    a.push(tmp);
                }

                returnValues.push(a);
                    

                return resolve.apply(this, returnValues);
            });
        });
    },
    /**
     * Create a new invoice.
     * 
     * @param {object} options
     * @return {this}
     */
    create: function(options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices";

        var data = {
			'customer_id': this.getCustomerId(), 
			'name': this.getName(), 
			'amount': this.getAmount(), 
			'currency': this.getCurrency(), 
			'metadata': this.getMetadata(), 
			'details': this.getDetails(), 
			'statement_descriptor': this.getStatementDescriptor(), 
			'statement_descriptor_phone': this.getStatementDescriptorPhone(), 
			'statement_descriptor_city': this.getStatementDescriptorCity(), 
			'statement_descriptor_company': this.getStatementDescriptorCompany(), 
			'statement_descriptor_url': this.getStatementDescriptorUrl(), 
			'return_url': this.getReturnUrl(), 
			'cancel_url': this.getCancelUrl()
        };

        var req = request.post(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var body = response.body;
                body = body['invoice'];
                        
                returnValues.push(cur.fillWithData(body));

                return resolve.apply(this, returnValues);
            });
        });
    },
    /**
     * Find an invoice by its ID.
	 * @param string invoiceId
     * @param {object} options
     * @return {this}
     */
    find: function(invoiceId, options) {
        if (!options) options = {};
        this.fillWithData(options);

        var request = new Request(this._client);
        var path    = "/invoices/" + encodeURI(invoiceId) + "";

        var data = {

        };

        var req = request.get(path, data, options);
        var cur = this;
        return new Promise(function(resolve, reject) {
            req.on('complete', function(result, r) {
                if (result instanceof Error)
                    return reject(result);

                var response = new Response(result, r);
                var err      = response.check();
                if (err != null)
                    return reject(err);
                
                var returnValues = [];

                
                var body = response.body;
                body = body['invoice'];
                        
                returnValues.push(cur.fillWithData(body));

                return resolve.apply(this, returnValues);
            });
        });
    },
    
}, {});
